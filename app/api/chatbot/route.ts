import { NextResponse } from 'next/server';

export async function POST(req: Request) {
  // Placeholder for AI chatbot integration
  return NextResponse.json({ message: "AI chatbot endpoint coming soon" });
}